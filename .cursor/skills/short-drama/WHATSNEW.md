**v1.41.0**（2026-06-11）

修复：同一角色连续发言被逐句拆成重复 cue（如 `角色名：句1` 紧跟 `角色名：句2`）的格式降级。规则收紧为：同形态连续发言必须合并为一条台词 cue，仅 cue 形态切换（台词/OS/VO 互切）或被动作、音效、他人台词等非台词元素隔开时才允许同名另起；配套修正输出模板示例占位与速查说明。

新增：`scripts/episode_validate.py` 集级格式机械校验（场景头/元数据/边界标记/CONTINUITY/钩子预告/破折号/裸 cue/相邻同名 cue 等 16 类 HARD + 5 类 WARN）。`/分集` 保存后自动以 warn 模式运行，发现 HARD 格式缺陷时列出清单建议修复，不阻断创作流程。

输入 `/新功能` 可随时回看本说明。

---

**v1.40.14**（2026-06-10）

修复：收窄对白段落边界规则的作用域。该规则现在只约束“剧本正文”内的 speaker cue，并明确源 Markdown 段落、扫描起止范围、同声/重叠/群众反应的替代写法，以及引用块、表格、模板示例、前情提要、自检报告、问题样例、考据附录等排除项，避免把格式协议误伤成创作限制。

工艺增强：吸收竖屏短剧信息密度与反 AI 腔口语化规则，强化人物出场信息、段落推进变化、事件钩、书面连接词降噪、无意义重复对白和平台口径未确认时的合规保守项。该增强落在 craft/audit 层，不把“每 15 秒反转”等经验写成机械硬规则。

输入 `/帮助` 查看全部命令

---

**v1.40.12**（2026-06-05）

修复：按分拆前 `8493f51^` 的结构恢复 `/分集` 主入口内联协议，不再只用一句话跳转到 `command-episode.md`。恢复内容保留后续已确认修复：ai_live 长台词/微表情仍按风险评估，首集/爽点/锚点不回退为一刀切硬合同，同时保留 v1.40.11 的对白段落边界规则。

输入 `/帮助` 查看全部命令

---

**v1.40.11**（2026-06-05）

修复：补齐 `/分集` 分拆后暴露的对白段落边界回归风险。每个对白 / OS / VO cue 必须独立成 Markdown 段落，同一段落出现第二个 speaker cue 判为格式错误；连续同一角色发言可合并为一条台词或拆成多个独立段，但不得把多个 `**角色名**：` 粘在一段里。首稿生成也会前置读取最小段落颗粒规则，避免导出和预览时对白粘连。

输入 `/帮助` 查看全部命令

---

**v1.40.10**（2026-06-03）

修复：全面清理创作层过度死板的硬规则。首集开场、爽点数量、三锚点、对白工艺、比喻堆叠从“一刀切阻断/必重写”改为“路径化风险评估”：传统爽文/IAA 路径仍严格看留存，精品化、悬疑、情绪慢钩路径允许用等价情境钩、信息钩或情绪钩替代。`dramatic-truth.md` 去除 demo 角色 Sarah 的全局硬套用，改为通用对白风险规则。

输入 `/帮助` 查看全部命令

---

**v1.40.9**（2026-06-03）

修复：重新评估 AI 仿真人剧承制规则，删除“单条台词 ≤2 句”和“微表情禁用”的绝对硬约束。ai_live 现在按真人表演节奏和模型可控性评估：常规对白仍优先短句，但长台词只在连续 3 句以上或单行约 90 字以上标为节奏风险；微表情改为高风险/谨慎使用/动作翻译三级处理。

输入 `/帮助` 查看全部命令

---

**v1.40.8**（2026-06-01）

修复：QA Calibration 后统一 state 和下沉协议口径。`/新建` 明确写 27 字段 stub state；全局 RMW 清单补齐 `/选题会`、`/圆桌诊断`、`/导出`；`/导出` 梗概缓存写入时机统一为成功综合后自动写入；`/选题会` 在 MCP 不可用时以 roundtable protocol 的 `/配置数据` 结束提示为准。

输入 `/帮助` 查看全部命令

---

**v1.40.7**（2026-06-01）

内部优化：继续瘦身 `/分集` 主流程，把加载路由、首集设计卡、medium 分化、连续性台账、used-lines 写入和结束提示下沉到 command-episode protocol；命令行为不变。

输入 `/帮助` 查看全部命令

---

**v1.40.6**（2026-05-31）

内部优化：继续瘦身 `/角色开发` 主流程，把分批状态机、恢复检查、finalize 合并、validator 命令和结束提示下沉到 command-character-dev protocol；命令行为不变。

输入 `/帮助` 查看全部命令

---

**v1.40.5**（2026-05-31）

内部优化：继续瘦身 `/选题会` 和 `/圆桌诊断` 主流程，把人物召集、四轮讨论、处方块、文件管理和 MCP 数据增强细则下沉到 roundtable protocol；命令行为不变。

输入 `/帮助` 查看全部命令

---

**v1.40.4**（2026-05-31）

内部优化：继续瘦身 `/导出` 主流程，把入口提示、内容选择、单集/范围导出细则下沉到 export protocol；同时移除单集导出里不存在的 `--force` 提示，避免用户按无效参数操作。

输入 `/帮助` 查看全部命令

---

**v1.40.3**（2026-05-30）

内部优化：按 skill-creator 的 progressive disclosure 原则瘦身 `/自检` 主流程，把评分速查表、checks 摘要模板和结束提示分支下沉到 reference 文件；`/自检` 行为不变，仍由独立质检 agent 执行。

输入 `/帮助` 查看全部命令

---

**v1.40.2**（2026-05-30）

更新：`/自检` 现在必须由独立质检 agent 执行评分和严重项判定；`--fix` 修复后也会重新启动独立质检 agent 复审，避免调度端自评自己刚写或刚改的剧本。

输入 `/帮助` 查看全部命令

---

**v1.40.1**（2026-05-22）

修复：优化 AI 真人剧和首集开场示例文案，减少不可拍的微表情、破折号和过满台词，让示例更贴近可拍动作与短剧口语。

输入 `/帮助` 查看全部命令

---

**v1.39.3**（2026-05-21）

修复：`/角色开发` 生成声音指纹前会先执行声纹工艺规则，避免把角色小传、作者旁白、高光宣言伪装成 voice 样本。voice 样本现在要求标清“对谁 / 什么压力 / 真实意图”，禁用项改用触发情境 / 禁用误写 / 替代路径 / 豁免条件格式；validator 会输出 `[声纹质量提醒]`，WorkBuddy 不会再把结构通过误报成声纹质量也通过。

输入 `/帮助` 查看全部命令

---

**v1.38.7**（2026-05-21）

**重要修复（Windows 用户）**：install.ps1 写入 `mcp.json` 时，PowerShell 5.1 的 `-Encoding UTF8` 默认会在文件头加 UTF-8 BOM（`EF BB BF`），WorkBuddy / Claude Code 的 JSON 解析器不接受 BOM 会失败，最终用户感知为「网址错了 / 连不上 / 405 nginx / SSE 无效内容类型」。本版改用 .NET API 写入无 BOM 的 UTF-8，并兼容老版本已写入的 BOM 文件（读取时自动剥）。

附带修复：
- 老 BOM 文件自动修复：重跑安装时检测到旧版本写入的 BOM 头会强制重写剥离，**即使 key 未变也会刷新文件**（修复了 idempotent 跳过分支让老文件永远修不好的盲点）
- Key 输入末尾自动剥 `\r` 与空白，粘贴时多带一个回车不再静默生成错 Key
- 安装完成后显式输出写入文件路径 + MCP endpoint URL，方便用户对照官方后台
- install.sh：同步加 Windows Git Bash 用户的 `\r` 防御

老用户修复方法：重跑 `irm https://raw.githubusercontent.com/MarkQWu/drama-workshop-skills/main/install.ps1 | iex` 一次即可（原 key 自动保留，BOM 自动剥离）。

---

**v1.38.6**（2026-05-20）

修复：/策划 的 MCP opt-in 从命令末尾移到「入口执行」位置（入口软提示之后、生成内容之前），模型现在会在开始 11 步之前先询问是否调用 MCP，不再跳过。

---

**v1.38.5**（2026-05-20）

修复：/选题会 /策划 MCP 降级提示拆分两条路径——MCP 未配置时才附 `/配置数据` 提示；用户已配置 Key 但主动跳过（选 N）时，完成行不再重复提示配置。

---

**v1.38.4**（2026-05-20）

修复：/选题会 和 /策划 在 MCP 未配置时，降级提示从「末尾追加」改为绑定到 `[完成]` 行，防止模型跳过输出。未配置 Key 时，完成行末尾会附带 `/配置数据` 提示入口。

---

**v1.38.3**（2026-05-20）

修复：用真实剧本文件测试发现的两处排版问题。① `> 前情提要 / 本集关键词` 等 blockquote 注释行现在会剥掉 `>` 前缀，以 10pt 小字显示（不再把 `>` 符号打进正文）。② `<!-- ... -->` HTML 注释行直接跳过不写入 Word（用于剧本内部标记，不应出现在交付文件中）。

输入 `/帮助` 查看全部命令

---

**v1.38.2**（2026-05-20）

内部：修复测试套件 `test_export_docx.py` 中过时的 `[样式]` 断言（该输出属于已废弃的 pandoc 路径）。测试 41/41 全部通过，对用户无影响。

输入 `/帮助` 查看全部命令

---

**v1.38.1**（2026-05-20）

修复：`/导出` 时若本机未安装 `python-docx` 或 `lxml`，现在会自动通过 `pip` 安装，安装成功后继续生成 Word 文件，无需用户手动操作（与之前 pandoc 自动安装行为一致）。

输入 `/帮助` 查看全部命令

---

**v1.38.0**（2026-05-20）

Word 导出引擎从 pandoc + reference-doc 切换为纯 python-docx，**不再依赖 pandoc**。新引擎逐行解析剧本 Markdown，对场景标题（`## N-M`）、集标题（`### 第N集`）、△ 动作描写、对白（`**角色名**`）、括号场景指示（`（BGM）`/`【闪回】`）分别应用专属字号和间距；正确设置中文字体（`w:eastAsia` 属性，防止 Word 将宋体替换为 Calibri）；全文 宋体/黑体 12pt 加粗，1.5倍行距，匹配专业剧本排版标准。依赖由 `pandoc（外部二进制）` 简化为 `pip3 install python-docx lxml`，跨平台安装更稳定。

输入 `/帮助` 查看全部命令

---

**v1.37.2**（2026-05-20）

修复 Word 导出排版混乱问题。`/导出` 生成的 `.docx` 中 △ 场景描写、对白、括号、闪回等行现在每行独立成段（不再被 pandoc 合并成一整段）；Normal 段落样式追加 6pt 段后间距，整体呼吸感更好；场景标题（`## N-M`）字号从 18pt 调整为 14pt，与集标题（`### 第N集`）拉开层级。修复对所有平台（Mac / Windows / Linux）的用户均生效，无需重装 skill。

输入 `/帮助` 查看全部命令

---

**v1.37.1**（2026-05-20）

`constraint-design.md` 仓库范例表格更新：将 `villain-design.md` 和 `vertical-drama-craft.md` 的评级从"待修"更新为"✅ 合格（v1.37.0 修复）"，追加靠山倒戈 3 条目，同步审查结论。

输入 `/帮助` 查看全部命令

---

**v1.37.0**（2026-05-20）

修复 `villain-design.md` 和 `vertical-drama-craft.md` 中的纯禁止约束结构。隐藏反派使用警告（4 条）和靠山倒戈禁止写法（3 条）升级为 B 类约束格式，每条包含触发情境 + 替代路径 + 豁免条件 + 判定测试，不再只写"不要 X"。台词禁止清单（6 条）升级为 A/C 类约束格式，每条补充具体替代路径，标注约束分级。执行 `constraint-design.md` 约束有效性设计原则（v1.36.0 建立），完成全仓库 B 类约束修复 Phase C1/C2。

输入 `/帮助` 查看全部命令

---

**v1.36.0**（2026-05-20）

新增角色约束有效性设计原则（`constraint-design.md`）：所有 B 类角色约束（声音禁区、行为约束）必须包含触发情境 + 替代行为 + 豁免条件，不再写纯禁止结构。`/角色开发` 模板新增 **应激模式** 字段（应激反应链表格，替代"绝不 X"描述），按场景给出角色的典型反应路径和弧线豁免条件。`/自检` 和 `/分集` 新增 `characters.md` 加载：角色禁区/应激模式违反时双步核查——有 ledger 事件支撑则弧线兑现不阻断，无记录则骨架层修复提示。

输入 `/帮助` 查看全部命令

---

**v1.35.2**（2026-05-20）

内置更新提醒改回 v1.33.0 风格：以"接入网文大数据 MCP"为主体，去掉后续细化版本的冗余描述。

输入 `/帮助` 查看全部命令

---

**v1.35.1**（2026-05-20）

`/自检` 长项目加载优化：项目集数超过 10 集时，优先读取 `continuity-ledger.md` + 目标集正文，不再默认加载全部历史正文，与 `/分集` 的 N>10 策略对齐。同步修正 `evals/assertions.md` 中两处指向已拆分模板文件的旧路径。

输入 `/帮助` 查看全部命令

---

**v1.35.0**（2026-05-19）

`/市场` 命令正式更名为 `/短剧市场`，避免与其他 skill 命令混淆；调用方式简化为只填平台名，时间范围和题材在命令后交互选择。`/配置数据` 修复：Key 现在同时写入 skill 目录和 WorkBuddy connector 注册表，避免新对话中 MCP 连接失效。MCP 连接协议修正，解决 WorkBuddy 卡在"授权中"的问题。

---

**v1.34.1**（2026-05-19）

`/帮助` 命令重构：按 skill 分 6 个区块（原创流程 / 改写&复刻 / 数据工具 / 分镜&视频 / 项目管理 / 其他），`/选题会` 和 `/策划` 旁明标 MCP 实时数据可选调用。

---

**v1.34.0**（2026-05-19）

新增 `/配置数据` 命令：在对话里粘贴网文大数据 Key 即完成配置，无需命令行或手动编辑文件。配置完重启 WorkBuddy / Claude Code 后，`/短剧市场`、`/选题会`、`/策划` 数据功能即可使用。

输入 `/帮助` 查看全部命令

---

**v1.33.0**（2026-05-19）

接入网文大数据 MCP：安装时自动配置 API Key，`/选题会` 和 `/策划` 可选调用实时榜单数据辅助创作决策；新增 `/短剧市场` 命令，输出 6 板块市场调研报告（番茄/红果/抖音漫剧实时榜单 · 题材分布 · 人设热力 · 金手指 · 改编信号 · 操盘建议）。

**配置 MCP Key（首次使用需做一次）：**
- Mac / Linux：终端运行 `bash install.sh --mcp-only`
- Windows：PowerShell 运行 `.\install.ps1 -McpOnly`

Key 免费注册：wangwendashuju.com → 个人中心 → API Key（首次赠 1000 Credits，5 月 31 日前有效）。配置后重启 Claude Code / WorkBuddy 即可使用。不配置不影响原有功能。

输入 `/帮助` 查看全部命令

---


**v1.32.1**（2026-05-19）

方法论文件条件加载：`/分集` 首次生成新集时不再加载 `vertical-drama-craft.md`（信息密度/钩子节奏）和 `dramatic-truth.md`（对白真实性诊断），这两个文件只在**重写/精修已有集**时按需读取；`script-element-extraction.md`（5 类元素分层 pipeline）从 `/分集` 移至 `/分镜`，仅分镜拆解时使用。写到第 50 集时，`/分集` 的支撑上下文进一步降至约 11,000 tokens（v1.32.0 基础上再降 35%）。

输入 `/帮助` 查看全部命令

---

**v1.32.0**（2026-05-19）

长剧上下文优化：`/分集` 现在会自动控制 `used-lines.md` 和 `continuity-ledger.md` 的体积。超过一定集数后，旧条目自动归档到单独的存档文件，活跃层保持固定大小；写到第 50 集时，支撑上下文从约 30,000 tokens 降至约 17,000 tokens，长剧创作更稳定。用户操作流程不变，继续按 `/分集 next` 使用，归档完全自动触发。

输入 `/帮助` 查看全部命令

---

**v1.31.33**（2026-05-19）

新增国内原创商业账本：`/策划` 会写入观众买单理由、付费卡点账本、爽点兑现账本和反派压力轨迹；`/分集目录` 仍以剧情推进为主体，并为每集标明剧情安排、主商业职责、副职责和观众继续看的理由；`/分集` 集末自查会补本集买单理由、尾钩压力、爽点兑现状态和反派压力变化；`/自检` 会把商业承诺漂移纳入爽点与主线诊断。

输入 `/帮助` 查看全部命令

---

**v1.31.31**（2026-05-19）

增强中文对白工艺：`/角色开发` 会生成更具体的声音指纹，`/分集` 写关键对白前按“像这个人 / 有潜台词 / 可动作替代”三问收束，`/自检 --fix` 优先删解释性、重复和直白对白，再做自然度与节奏修复。

输入 `/帮助` 查看全部命令

---

**v1.31.30**（2026-05-18）

补齐 helper CLI 的 `--help` 契约：`export_docx.py` 与 `generate_reference_doc.py` 现在可在系统 Python 3.9 下直接查看帮助并返回 0，且 `generate_reference_doc.py --help` 不再依赖预装 `python-docx`。用户使用流程不变。

输入 `/帮助` 查看全部命令

---

**v1.31.29**（2026-05-18）

继续补齐系统 Python 3.9 兼容：`/角色卡` 校验脚本和分镜合并脚本不再使用 Python 3.10 才支持的 `| None` 类型注解。用户使用流程不变。

输入 `/帮助` 查看全部命令

---

**v1.31.28**（2026-05-18）

修复 `/角色一致性` 脚本在系统 Python 3.9 下的类型注解兼容问题，确保本机默认 Python 也能运行角色一致性测试。用户使用流程不变。

输入 `/帮助` 查看全部命令

---

**v1.31.27**（2026-05-18）

收紧连续性台账的沙盒验证口径：老项目必须先 bootstrap 再续写，`/导出 --with-bible-ref` 明确保留考据附录但剥离内部连续性块，eval 不再把未跑老项目 bootstrap 误判为完整通过。用户使用流程不变。

输入 `/帮助` 查看全部命令

---

**v1.31.26**（2026-05-18）

新增原创长篇连续性台账：`/分集` 会自动维护角色动态状态、主线伏笔和分集索引，写到 30/50/80 集时更稳地承接尾钩和角色状态。用户流程不变，继续按 `/分集 next` 使用；`/导出` 和 `/分镜` 会自动剥离内部连续性块，不污染交付物。

输入 `/帮助` 查看全部命令

---

**v1.31.25**（2026-05-15）

清理出海平台推荐表里的旧表述：将“英文”改为“英语市场/海外平台”，避免误读为 `/出海` 默认输出英文。`/出海` 仍默认中文呈现，只有明确 `/出海 英文交付` 才切换英文剧本格式。

输入 `/帮助` 查看全部命令

---

**v1.31.24**（2026-05-15）

调整 `/出海` 的默认交付方法：出海模式只切换内容规则，不再自动切到英文 + Hollywood 格式。默认写入 `language=zh-CN`、`scriptFormat=cn-shortdrama`，用中文短剧格式和中文对白呈现海外剧本，角色名保留英文；只有明确输入 `/出海 英文交付` 或要求英文剧本格式时，才切换为 `language=en-US`、`scriptFormat=hollywood`。自检和 eval 也改为按 `scriptFormat` 判断对白/场景格式，避免国内创作者看不懂英文稿。

输入 `/帮助` 查看全部命令

---

**v1.31.23**（2026-05-15）

修复出海资料分层后的命令接线遗漏：`/开始`、`/角色开发`、`/分集目录`、`/导出`、`/合规` 现在按 mode 读取对应海外资料；新增出海分集目录模板；降级 anti-domestic 与 vertical-filmability 的文件级门控，避免 review/craft 规则误当 must-rewrite；清理 `WIDE SHOT` 旧 eval 口径，统一出海视觉锚点为 `VISUAL ANCHOR`。

输入 `/帮助` 查看全部命令

---

**v1.31.22**（2026-05-15）

重排出海具体资料层：`hard-rules.md` 改为 HARD BLOCK / REVIEW / STRONG GUIDANCE 分级，避免经验判断误升为自动阻断；对白资料拆成平台规则、通用工艺、长剧参考风险三层；反污染资料拆成国内迁移、结构导入、竖屏可拍性和合规/IP 风险，`/出海` 加载清单与 reference registry 已同步。

输入 `/帮助` 查看全部命令

---

**v1.31.21**（2026-05-15）

修正出海分层资料加载说明：`/分集` 与 `/自检` 不再写死“全部 4 文件”，统一指向 `/出海` 的完整分层资料清单，避免后续新增资料层时维护说明滞后。

输入 `/帮助` 查看全部命令

---

**v1.31.20**（2026-05-15）

Overseas Reference Cleanup：清理出海资料污染并建立市场因果分层。新增 `references/overseas/layer-index.md` 和 `compliance-risk.md`；`/出海` 先按分层读取平台事实、目标市场、本土文化、题材承诺、结构、媒介、台词和合规/IP 风险。移除废弃首集 5-beat 对 hard gate 的污染，Gwen Hayes 降级为 romance scaffold，出海策划不再使用三幕/爽点矩阵/固定付费卡点模板，出海分集模板不再默认 `WIDE SHOT`。

输入 `/帮助` 查看全部命令

---

**v1.31.19**（2026-05-14）

为原创 `short-drama` 补上三层控制模型：新增 `references/three-layer-control.md`，明确原创创作按 `地基层 100% / 骨架层 75% / 血肉层 35%` 执行。`/策划`、`/角色开发`、`/考据`、`/分集目录`、`/分集`、`/自检`、`/圆桌诊断`、`/角色一致性`、`/导出`、`/分镜` 在判断阻断或修改建议时，必须区分 `[地基层阻断]`、`[骨架层修复]`、`[血肉层建议]`，防止把创作偏好误升为 hard gate。

输入 `/帮助` 查看全部命令

---

**v1.31.18**（2026-05-14）

补清 `/出海` 模式边界：项目管理、state、承制介质、角色一致性、台词去重、导出等基础设施继续共用；语言、对白格式、场景模板、题材映射、首集设计、付费墙、平台 runtime、文化禁区和合规判断必须按 mode 分流。通用规则与海外专属规则冲突时，以 `references/overseas/` 为准。

输入 `/帮助` 查看全部命令

---

**v1.31.17**（2026-05-14）

修复 `/出海` 项目的首集流程：海外模式写第 1 集时不再套用国内 Ep1 设计卡、冷开场合同、事件钩首集硬约束或首集传播句提取，而是按海外 hard rules 与 anti-patterns 规则直入 active conflict / mid-conflict。

输入 `/帮助` 查看全部命令

---

**v1.31.16**（2026-05-13）

修复 `/更新` 的用户路径：普通用户仍只需要在 agent 里输入 `/更新`，或接受自动更新提示；更新过程不再向用户暴露底层安装细节。安装底层改为工具无关的统一版本源，Claude / Codex / OpenClaw / WorkBuddy 共用同一份本地 skill，避免多 agent 复制目录导致版本漂移。

输入 `/帮助` 查看全部命令

---

**v1.31.15**（2026-05-11）

继续补强 release readiness gate：新增安装器契约检查，自动确认 `install.sh` / `install.ps1` 指向 `drama-workshop-skills`、使用 `.skill-trash` 安全备份、迁移旧 `skills/.trash`、按 `SKILL.md` discovery 安装仓库内 skills，并避免破坏性删除命令。不迁移 runtime。

输入 `/帮助` 查看全部命令

---

**v1.31.14**（2026-05-11）

补强 release readiness gate：新增 frontmatter、帮助清单、更新仓库名策略和 skill discovery 自动检查，并修复 `SKILL.md` frontmatter 引号格式，使 `description` 可被闸门解析。不迁移 runtime。

输入 `/帮助` 查看全部命令

---

**v1.31.13**（2026-05-11）

补齐 v1.31.12 的独立审查修复：`/分集` 与 `/自检` 显式加载 `creative-intent-ledger.md`，让原始冲动记录的 read contract 与命令加载清单一致；release-gate 单测改用临时 runtime fixture，不再依赖本机 HOME 下的 runtime 副本状态。不迁移 runtime。

输入 `/帮助` 查看全部命令

---

**v1.31.12**（2026-05-11）

新增 `references/creative-intent-ledger.md`，并在 `brainstorm.md` 模板中加入「原始冲动记录」字段：原始前提、核心关系、爽感引擎、结局偏好、不可牺牲点。同时在 `reference-layers.md` 明确 hard gates、soft rubrics、creative guidance、template contracts 的职责边界，避免把创作偏好误当阻断门槛。不迁移 runtime。

输入 `/帮助` 查看全部命令

---

**v1.31.11**（2026-05-11）

重构 `references/quality-rubric.md` 顶部总纲，明确 `/自检` 先判定 hard gates，再做商业生命力评分，最后执行 anti-greenlight 机制。触发阻断门槛时总分只保留诊断意义，不能洗白阻断结论；craft 层规则默认仍是软评分，只有 OOC、违规、不可拍、媒介不匹配或事实矛盾时升级阻断。不迁移 runtime。

输入 `/帮助` 查看全部命令

---

**v1.31.10**（2026-05-11）

为首批 hard-gate references 增加维护者 metadata：`layer`、`control`、`authority_id`、`canonical_path`、`read_when`。覆盖格式、项目状态、合规、考据、更新机制、承制介质和海外硬规则，便于后续把阻断门槛与软评分拆开。不改变用户命令行为，不迁移 runtime。

输入 `/帮助` 查看全部命令

---

**v1.31.9**（2026-05-11）

新增 `references/reference-layers.md`，作为维护者使用的规则分层索引，先把现有 references 归到 foundation / structure / craft / modes / templates，后续用于 hard-gate metadata 和 quality rubric 重构。不改变用户命令行为，不迁移 runtime。

输入 `/帮助` 查看全部命令

---

**v1.31.8**（2026-05-11）

同步 short-drama-remake v0.4.0 的 `/仿写` 子命令入口说明。

- `/仿写` 仍是兼容跳转入口，真正拆解复刻能力在同级 `short-drama-remake`。
- 新增用户可见入口提示：`/仿写 帮助`、`/仿写 状态 PROJECT_DIR`、`/仿写 继续 PROJECT_DIR`。
- 明确原创主流程 `/开始` 和复刻项目恢复入口分开，避免新对话恢复时误进原创项目列表。

**v1.31.7**（2026-05-11）

修复 WorkBuddy 中 `/更新` 被误当成“更新项目状态”的路由问题。现在 `/更新` 被定义为最高优先级精确命令，只能执行仓库级 skill 更新，不得生成剧本项目状态表或推进当前剧本项目。

---

**v1.31.6**（2026-05-10）

安装指引改为 git + 全局代理路线。若安装器检测不到 git，会提示先安装 Git；若 GitHub 下载失败，会提示打开全局代理后重新运行安装命令。

---

**v1.31.5**（2026-05-10）

历史说明：此版本曾短暂尝试另一种下载策略；v1.31.6 起安装指引已改回 git + 全局代理路线。

---

**v1.31.4**（2026-05-09）

`/更新` 已改为仓库级更新：会从 `drama-workshop-skills` 拉取最新版并重新安装仓库内全部 sibling skills，包括 `short-drama` 和 `short-drama-remake`。同时清理旧仓库名引用。

---

**v1.31.3**（2026-05-08）

WorkBuddy 安装修复：安装脚本会自动把 `skills/.trash` 迁移到同级 `.skill-trash/`，避免 WorkBuddy 递归扫描旧版 `short-drama/SKILL.md`，导致 `/仿写` 仍进入旧流程。重装后请关闭/移除当前 WorkBuddy 工作空间项目并重新打开。

---

**v1.31.2**（2026-05-08）

`/仿写` 兼容入口修复：用户继续输入 `/仿写` 时，不再进入旧项目扫描流程；会优先加载同级 `short-drama-remake`，直接执行新版短剧拆解复刻能力。只有未安装 `short-drama-remake` 时，才提示重装最新版短剧 Skill。

---

**v1.31.1**（2026-05-07）

升级提醒加强：输入 `/开始`、`/更新` 或任意短剧命令时，若检测到旧版本，会提醒更新。历史说明：此版本发布时，旧安装器需要重装才能补齐新版独立 `/仿写` 能力；v1.31.4 起 `/更新` 已升级为仓库级更新。

请让你的智能体执行：
“帮我重装最新版短剧 Skill，并确认新版仿写能力已安装成功。”

剧本正文破折号完全禁用：`——`、`—`、`--` 任意出现均标【严重】。停顿、被打断、补充说明、心理独白引出，改用动作描写、换对话轮次、完整句、句号、逗号或中文省略号「…」。

---

**v1.31.0**（2026-05-07）

新版 `/仿写` 已升级为独立的短剧拆解复刻能力，重点支持长剧本拆分、可复刻骨架、10 个换皮方向、项目策划、详细集纲和正式短剧剧本。

历史说明：此版本发布时需要重装才能补齐新增能力；v1.31.4 起 `/更新` 已升级为仓库级更新。若仍缺少 `short-drama-remake`，请让你的智能体执行：
“帮我重装最新版短剧 Skill，并确认新版仿写能力已安装成功。”

安装完成后，关闭当前会话并重新打开，再继续使用新版 `/仿写`。

---

**v1.30.0**（2026-05-03）

新增大文件仿写支持：50集以上的源剧本现在会自动压缩处理，无需手动拆分提供

---

**v1.28.2**

新增 `/新功能` 命令：随时查看最近一次更新了什么

---

输入 `/帮助` 查看全部命令
