# 质量规则（Quality Rules）

本文件为跨介质通用规则。执行前先读 `.drama-state.json#medium` 按介质补充加载：
- `medium="ai_live"`（默认 · 仿真人 AI 剧）→ 额外加载 `ai-live-rules.md`（场次 3-5 / 长台词按节奏风险评估 / VO ≤20% 字数 / 微表情按可控性评估）
- `medium="comic"`（漫剧）→ 额外加载 `comic-rules.md`（场景 ≤3 / 单条台词 ≤6 句 / 场景头新格式 / OS/VO 情绪标签）

## 分集质量要求

- 每集 800 字以上（中文）/ 600 words+（English）
- 景别提示：全景、中景、近景、特写（至少使用3种）
- 台词带语气或动作指示
- 每集结尾必须有悬念钩子（参考 hook-design.md）
- 第1集必须在前30秒（约前3段）抓住观众（参考 opening-rules.md）
- 付费卡点集（[付费]）结尾必须制造强悬念
- 角色称呼必须与 characters.md 称呼关系表一致（参考 realism-checklist.md）
- 角色认知转变必须有具体触发物（看到/听到/撞见），不能凭"感觉"空降结论
- 私人关系通过具体生活场景呈现，不用标签式描述（"多年好友"→具体互动）
- 情绪/立场转折必须有至少一个中间过渡状态，不能一步到位
- 正式场景（面试/谈判/汇报）台词须符合场景基本逻辑，允许戏剧化但不能违反常理
- 默认扫描重要人物出场是否携带有效信息（身份 / 处境 / 关系 / 目标 / 压力 / 行动方式之一）。只报名字和外貌时标 `[骨架层修复]`；只有破坏本集职责、尾钩义务、可拍性或合规时升级阻断
- 默认扫描连续小段落是否有压力、信息、关系或选择变化。互骂和重复追问不算推进；通常标 `[骨架层修复]` 或 `[血肉层建议]`，不单独作为 hard gate
- 中文对白默认扫描书面连接词、解释性废话和无意义重复，具体规则见 `dialogue-craft-cn.md#反-ai-腔与口语化边界`

### 反抽象 & 画面可拍性规则（轻约束）

**设计意图**：让场景叙事层（摄影机能看到的层）落地可拍，同时保留 OS/VO 层承载诗意想象力（anchor 机制的红利落在这里）。**不是关键词黑名单**——用分层原则 + 3 问自检 + 翻译示范，让模型自己判断。

#### 分层原则（4 层，约束梯度越靠近摄影机越严）

| 层 | 识别标志 | 约束 |
|---|---|---|
| **场景叙事层** | `△` 开头 / 景别标注（出海优先 `VISUAL ANCHOR`，老格式兼容 `WIDE SHOT` / `MEDIUM SHOT` / `CLOSE-UP`）开头 / `（BGM：...）` / `（音效：...）` / `[音乐]` 提示 / `【闪回】…【闪出】` 段落 | **必须可拍**（物件/动作/环境/表情）。**禁用第一人称感受动词**（感觉/觉得/意识到/感受到）—— 命中即判越界，强制翻译成 OS/VO 或生理反应 |
| **OS 内心独白** | `**角色**（OS）:` | 单段（连续同一 OS 块）内 **≤1 核心比喻**，不跨段累加；单段 ≤3 句；**禁止** "看不见的 X / 胸腔里" 反直觉画面 |
| **VO 旁白** | `**角色**（VO）:` | 每集 VO 总字数 ≤ 本集总字数 **20%**；单段 ≤3 句；**不得替代**场景叙事层的可拍性职责（不允许"场景层抽象 → VO 补解释"分工） |
| **对白** | `**角色**：` | 适用现行对白规则，本节不覆盖 |

**闪回/幻觉段落** 仍归场景叙事层处理——不给 mate bond 幻觉 / 梦境 / 通灵场景的"像电流穿过脊椎"类描写开逃生通道。

#### 4 类反抽象模式

场景叙事层 + OS 超阈位置，扫描以下 4 类：

| 类 | 模式识别 | 典型反例 → 翻译方向 |
|---|---|---|
| **原类** | 纯情绪/氛围词（无物理表现） | "她很难过" → "眼眶湿润，咬着下唇"<br>"气氛凝重" → "两人对视，男主握紧桌沿"（参考 storyboard-guide.md 情绪→物理动作对照表） |
| **A 抽象情绪物化** | 把不可见概念当物体描述其物理行为 | "空气已经绷紧了" → "前排孩子被家长按住肩膀，没人说话"<br>"压迫感从他身体里溢出来" → "他每下一步，前排人群就后退半步" |
| **B 通感/多层比喻** | 跨感官比喻 / 摄影机无法对应视觉的比喻 | "沉闷，像心跳" → "低音厚重，每击留三秒静默"（音色+节奏 brief）<br>"像水结成冰的过程" → "他眼中那一丝惊讶褪去，变冷。嘴角绷紧一分" |
| **C 不可见内心画面** | "看不见的 X" / "胸腔里" 等人体内部不可摄描述 | "胸腔里某根看不见的线被拉紧" → 拆层：场景留生理反应（"瞳孔骤然收缩，倒吸一口气"）+ OS/VO 承载概念（"……Mate Bond。"）|

**A 类可查 storyboard-guide.md 情绪→物理动作对照表**（有情绪词直接映射动作）。
**B/C 类需创作性翻译，无查表路径**——模型自己基于场景语境重写。

#### 3 问判定（写完 △ 段落后自检 / /自检 扫描）

场景叙事层每一句过 3 问：

1. **Q1（硬扫描）**：摄影机能拍到这句说的东西吗？（物件/动作/环境/表情？）
2. **Q2（半硬扫描）**：如果是不可见的东西（情绪/感受/关系），是否已翻译为人的外部反应？
3. **Q3（软提示，用于 --fix 建议生成）**：如果是比喻，本体和喻体都能拍出来吗？（"像干裂的墙皮" OK——人脸 + 墙皮都可感；"像水结冰" NOT OK——眼神变化无法对应"水结冰"视觉）

任一失败 → 标记 → 给翻译建议。

#### 扣分机制（单集上限 -3，参考破折号规则 L94）

- **维度 6（反抽象与镜头化）**：场景叙事层 + OS 超阈的 A/B/C 类违规每处 -1，**单集合计上限 -3**
- **维度 7（AI Slop）联动**：**单集场景叙事层比喻句 ≥4 处 -2**（解决"单句过 3 问但堆叠成灾"—— 如一集里"像一粒灰尘/像墨水洇开/像蜡烛闪烁/像烛芯烧尽"连发）；对白 + VO 超阈归此维度
- **C 类与"上帝视角"不重叠扣分**：C 类 = 单句不可见画面；上帝视角 = 跨场景叙述剧中人不可知。同一句不重复扣分

#### /自检 新增扫描规则

在现有"纯情绪词扫描"之外，新增 A/B/C 3 类识别（判定标准见上表反例）。详细判定边界见 `quality-rubric.md#六-反抽象与镜头化#纯情绪词判定边界` 的 A/B/C 追加段。

### 台词风格校验

每句台词必须对照 characters.md 角色-语言风格映射表和声音指纹。霸总不会长篇大论解释心情，小白兔不会突然气场两米八。中文对白工艺见 `dialogue-craft-cn.md`：遮住角色名仍能听出是谁；表面意思与真实意图有差；能用动作、道具、沉默替代时优先删对白。

### 台词长度控制（按 medium 分化）

单条台词长度控制按 `.drama-state.json#medium` 分化：
- `medium="ai_live"`（默认）→ 不设 2 句硬上限；连续 3 句以上或单行约 90 字以上标为节奏风险，详见 `ai-live-rules.md#单条台词长度`
- `medium="comic"` → 最多 6 句（漫剧分镜可承载长台词），详见 `comic-rules.md`

跨介质通用：旁白/独白也不超过 3 句（单段约束，见上方"反抽象 & 画面可拍性规则" L28 的 VO 单段 ≤3 句）。

### 对白与叙事语言规则（分化版）

**触发条件**：执行前读取 `.drama-state.json#mode` 与 `.drama-state.json#scriptFormat`。`mode` 决定内容规则；`scriptFormat` 决定对白和场景格式。`scriptFormat` 缺失时默认 `cn-shortdrama`。

#### 中文短剧格式（scriptFormat: cn-shortdrama 或缺失）

**对白格式（强制）**：

- 唯一合法格式：`**角色名**：台词内容`；对白前附语气/动作时：`**角色名**（语气描述）：台词内容`
- 出海中文呈现时，角色名保留英文，例如 `**Ava**（压低声音）：我不会签。`
- 剧本正文内，每个对白回合独立成源 Markdown 段落，前后空行隔开；同一段落内出现第二个 `**角色名**` / `**角色名**（...）` / OS / VO cue = 格式错误。连续同一角色的同形态发言必须合并为一条台词 cue；仅当 cue 形态切换（台词/OS/VO 互切）或被非台词正文元素（`△` 动作段、（BGM/音效）、其他角色台词、场景标题等）隔开时才允许同名另起 cue，禁止逐句重复同名 cue。
- 括号动作不逐句机械重复；连续状态用一条 `△` 动作段承载，或只在状态变化时更新括号动作。
- **禁止**双引号包裹对白内容：`"..."`（中文直角）、`"..."`（中文弯引号）、`'...'`（单引号）、`"..."`（英文直引号）——均不合法
- **例外**：角色"引用他人说过的话"的嵌套语境可用中文直角引号 `「」`（如 `**女主**：他当时说「你再等三天」`）
- **漂移防御**：△ 段落中若提到对白内容，改用间接引述（`**男主** 说要等三天`）或 `「」`

**示例**：
- ✅ `**男主**（冷声）：我等你三年了`
- ❌ `**男主**（冷声）："我等你三年了"`
- ✅ `**女主**：他说「你再等三天」，我就信了`

#### Hollywood 英文交付格式（scriptFormat: hollywood）

**对白格式（强制）**：好莱坞行业标准 `**CHARACTER NAME** (tone direction): "dialogue"` —— 必须使用英文直引号 `"..."`包裹对白内容。仅当用户明确要求英文交付/好莱坞格式时启用，不再由 `mode=overseas` 自动触发。

**示例**：
- ✅ `**JACK** (coldly): "I've waited three years for you."`
- ❌ `**JACK** (coldly): I've waited three years for you.`（缺引号，不合规）

#### 破折号规则（两种模式共用，强制）

**结论**：剧本正文禁止破折号。`——`、`—`、`--` 均不得出现。

**禁用范围**：

- 台词内部停顿/犹豫/被打断
- 场景描述补充说明
- 心理独白引出
- 出海模式英文对白中的英文破折号

**替代方式**：

- 停顿：用句号、逗号、换行或动作描写。
- 被打断：换对话轮次，用动作或另一角色插话形成打断。
- 补充说明：拆成完整句或独立动作描写。
- 心理独白：直接用完整句表达，不用破折号引出。

**/自检 扫描规则**：统计剧本正文中 `——` / `—` / `--` 出现次数。排除 `>` 引用块、`<!-- ... -->` HTML 注释、前情提要引用、集末自查、本集考据引用附录等元信息段。

- 0 次：合规
- ≥1 次：标【严重】，要求改写后才可通过 /导出

**常见改写示例**：

| 原问题 | 改法 |
|---|---|
| 台词未说完 | 改完整句，或让下一角色直接插话 |
| 停顿观察重复 | 动作已写停顿时，台词用句号断开 |
| 定语省略偷懒 | 把关键词说完整，再切换动作或对话 |

**设计理由**：用户明确反馈「破折号全部不要出现」。因此从“0 次首选、≤3 次容忍”升级为“剧本正文 0 次硬约束”。破折号问题不再做密度扣分或扣分上限处理，任意出现都按【严重】处理。

---

## 来源与迭代记录

2026-04-20 新用户（Windows WorkBuddy）两条独立反馈触发本规则：① 破折号加入指令作为小说化符号；② 人物说话不要双引号直接用冒号。规则设计兼顾出海好莱坞标准不被破坏。

2026-04-21 smoke test 剧本出现 3 处不必要的破折号（嘴硬式未说完 / 定语省略 / 停顿观察重复），用户反馈收敛：现有规则只有量化约束（2-5 次/集），没有必要性约束。新增「默认偏向完整句」4 问 + 3 种 AI 误用清单 + /自检 扫描扣分。核心原则：破折号默认该删，有明确理由才保留。

2026-04-22 用户反馈「剧本上破折号基本不需要出现」触发密度反转：密度下限 2 与生成层「默认偏向完整句」原则冲突（强制每集 ≥2 次），且自检输出「破折号密度（2-5次）✅ 4 次」被解读为"写破折号=合规"的反向信号。改为 0 次首选、上限 ≤3、无下限。4 问末问警戒线直接降到"≥1 次即警惕"（出现即进入默认 0 模式，与"基本不需要出现"哲学一致）。同时加 L87 扣分总上限 -3（格式维度清零风险审查发现）。

2026-05-05 用户反馈「破折号全部不要出现」触发规则升级：从“0 次首选、≤3 次容忍”改为“剧本正文 0 次硬约束”。`——`、`—`、`--` 任意出现均标【严重】，不再做密度扣分或扣分上限处理。停顿、打断、补充说明改用动作描写、换对话轮次、完整句、句号、逗号或中文省略号。

2026-04-22 用户反馈（WorkBuddy · overseas mode · werewolf / mate rejection 题材 ep001）标 5 条"小说化，画面拍不出来"批注（"空气绷紧" / "像心跳" / "压迫感溢出" / "看不见的线" / "像水结成冰"）+ 全文扫另有 5-6 处中度漏标。根因定位：原反抽象规则仅覆盖"纯情绪词→物理动作"一类，漏掉 3 类新模式（抽象情绪物化 / 通感比喻 / 不可见内心画面）。quality-rubric L322-324 判定边界"含物理表现就放过"误放过"压迫感溢出"等。扩展为 4 类模式 + 4 层分层原则（场景/OS/VO/对白，约束梯度越近摄影机越严）+ 3 问判定 + VO 容量上限（防规避自检把小说腔塞 VO）+ 单集比喻堆叠硬上限（维度 7 联动）。**轻约束设计哲学**：不做关键词黑名单（会压死 anchor 机制的想象力红利），用分层 + 翻译示范让模型自主判断。本规则跨 mode / 跨 anchor 激活状态通用，是基础硬门槛。

2026-04-22 生成层约束补丁：反馈"只靠自检约束多余一环"，原提议 inline 3 问循环 + 触发词列表经评估为 over-engineering（触发词会误伤"像干裂的墙皮"合法比喻 / 跨层污染合法角色判断语言 / 10-20 次 △ 循环抢 anchor 和节奏 attention / 与 /自检 重叠使事后兜底变弱）。改为在 `/分集` 命令段落加**一句话**分层提醒（"摄影机能拍到吗？OS/VO 允许诗意比喻，△ 必须可拍"），与破折号实时节制范式对齐。双层防线：生成层抽象提醒 + 自检层事后扣分。anchor 红利承载位（OS/VO）明示保护。

### 爽点密度风险评估

每集默认目标是 3 个爽点（参考 satisfaction-matrix.md 的 5 大类型）。写完后在集末自查清单中列出本集爽点及类型；不足 3 个标为留存风险，优先补强，但不自动判定"未完成"。

**有效爽点判定（v1.20.0 新增 · 防注水）**：本条计的是"爽点位置"，但要计入达标必须是**有效爽点**——见 `satisfaction-matrix.md#有效爽点判定` 4 选 3（痛点具象化 / 主角显性反应 / 局势变化 / 反派反应）。注水模式（仅身份暗示 / 仅道具露出 / 主角全程隐忍 / 仅 OS 内心）**不计**。第 1 集额外建议至少 1 个**完整爽点弧**（4 节点：痛点 → 蓄力 → 小释放 → 余震）。位置标了 5 个但有效仅 2 个 → 标为爽点注水风险，优先补强

### 秒级节奏锚点

每集默认包含以下三个节奏锚点，在剧本中用 `[锚点]` 标记：
- [锚点]**0-3s 冲突点**：开场前 3 段内优先出现冲突/悬念/反转；精品化路径可用情境钩替代，但必须给继续看的理由
- [锚点]**30s 情绪爆破**：中段优先有一个情绪高峰（打脸/揭秘/爆发），对应约第 2-3 场次
- [锚点]**结尾钩子**：最后一段优先是悬念钩子（已有规则，此处强调标记 [锚点]）

#### anchor 节奏污染追问（v1.15.0 新增）

`/自检` 检查节奏锚点维度时，如发现**缺失任一锚点**（0-3s 冲突 / 30s 爆破 / 结尾钩子）：
1. 检查 `creative-plan.md` 是否有 `anchor` 字段（v1.23.0 全 13 题材均可能有）
2. 如有 → 在 findings 中追问用户：「本集节奏锚点缺失，是否被 anchor《[anchor 名]》节奏污染？[Y/N]」
3. 用户回 **Y** → 建议执行 `/分集 N --fix anchor-rhythm` 重写本集（临时清空 anchor 重新生成）
4. 用户回 **N** → 按常规节奏锚点扣分逻辑处理

### anchor 同人化禁用词清单（v1.15.0 新增 · F4 失败模式兜底）

防止模型把 anchor 当成"必须模仿的对象"，疯狂塞 anchor 标志性词汇/原台词。`/自检` AI Slop 维度扫描以下禁用词，**本集出现 ≥1 次扣分 + 标 `[anchor 同人化]`**（如有 `[意图复用]` 注解则放行，参考 used-lines.md 例外机制）：

| anchor | 禁用词清单 |
|---|---|
| 后宫·甄嬛传 | 「臣妾做不到啊」「贱人就是矫情」「本宫」「嬛嬛」「钮祜禄」（核心人物名 + 标志性台词）|
| 何以笙箫默 | 「要么一辈子，要么一拍两散」「默笙」「以琛」（人物名 + 名场面台词）|
| 庆余年 | 「范闲」「叶轻眉」「庆帝」+ 直接复制粘贴中国古典诗词组合 |
| 平凡的世界 | 「孙少安」「孙少平」「田晓霞」「贺秀莲」（人物名直接出现）|
| 隐秘的角落 | 「朱朝阳」「严良」「普普」「张东升」「我们一起爬山吗」（人物名 + 名梗）|

**判定逻辑**：模型借调性时**不应**在剧本里直接出现 anchor 人物名 / 标志性台词。借调性的标志是"看不出借了哪部"——隐式吸收。如剧本里出现"甄嬛"或"范闲"作为本剧人物名，明显是 anchor 串味。

### AI 兜底形容词禁用词清单（v1.17.1 新增 · 非 anchor 串味）

与 anchor 同人化禁用词（上表）不同，这类 AI 兜底词是模型在**反差 / 情绪切换 / 身份伪装**场景的通用 AI 库存，不绑定某部 anchor。ep001 / 付费卡点等强情绪节点尤其高发。`/自检` AI Slop 维度扫描以下 5 类，**本集出现 ≥1 次扣分 + 标 `[AI 兜底词]`**：

| 类型 | 命中词/写法 | 改法方向 |
|---|---|---|
| 瞳孔描写 | 「瞳孔微微收缩」「瞳孔骤缩」「瞳孔一紧」「瞳孔放大」「瞳孔骤然收缩」 | 换成可见动作：「他看完，锁屏」/「他看了三秒」 |
| 眼神 N 式 | 「眼神骤然冷了一瞬」「眼神锐利如刀」「眼神如刀」「眼神锋利得像刀」「眼神冰冷」 | 换成具体动作：「他合上窗帘」/「他没看他们」 |
| 猎人 N 式 | 「猎人般的眼神」「猎人的眼神」「如狼如虎」「冰冷的、属于猎人的眼神」 | 删除比喻，用沉默 + 细节承载身份反差 |
| 囚徒比喻 | 「像个接受审判的囚徒」「像被判刑的人」「像待宰的羔羊」 | 删除"像 X"比喻句式，用身体语言：「他站着。没抬头」 |
| 嘴角 N 式 | 「嘴角没有任何弧度」「嘴角勾起一抹 X」「嘴角扬起一抹冷笑」 | 避免公式化嘴角描写；改为沉默、视线、动作或更具体的单句对白 |

**判定逻辑**：这 5 类是 AI 在"隐藏身份 / 反差爽 / 情绪爆破"场景的通用兜底库存。共同问题 = 用华丽形容词替代具体画面，摄影机拍不到"瞳孔收缩"（特写只能拍眼球，拍不到"收缩"这个概念）。与 §反抽象 & 画面可拍性规则 C 类"不可见内心画面"（L45）同源，但这 5 类高度模板化，机械扫描效率 >> 3 问判定。

**扣分归属（强制，覆盖 SKILL.md 维度 6 的"同句命中 A/B/C 优先归维度 6"默认）**：本清单 5 类命中 → 归**维度 7 AI Slop**（不走维度 6 A/B/C 的 3 问判定路径），每类命中 -1，**单集上限 -3**（与维度 7 AI Slop 对齐）。同句命中本清单 + 维度 6 A/B/C 时以本清单优先，不重复扣分。

**证据来源**：2026-04-23 战神归来 ep001 v4 版在 5 场戏中集中出现 5 处（「瞳孔微微收缩」/「眼神骤然冷了一瞬」/「冰冷的、属于猎人的眼神」/「眼神锐利如刀」/「像个接受审判的囚徒」），盲评 AI 痕迹密度 10/10 卡死质感。

**向后兼容提醒**：v1.17.1 起新增本清单扫描，历史已生成集数重跑 `/自检` 可能下降 -1 至 -3 分（属预期行为，非 bug）。

### Type 6. 身份关系陈词（v1.18.1 新增 · F5 配套）

**⚠️ 扫描 scope 限定（防止 /自检 假阳性）**：本类清单**仅在 `/导出 --docx` 人物小传合成步骤 4（身份与关系段生成）时启用**，作用于 LLM 合成过程的输出扫描。`/自检` 的正文扫描（质检每一集剧本）**不加载本类**——否则剧本正文合法使用"宿敌 / 欢喜冤家"描写反派关系会被误扣分，与本规则真实目标（防 LLM 合成 AI slop）相悖。Type 1-5 全局生效（/自检 + 导出均扫描），Type 6 仅导出 biography 合成路径生效。

**Exception 前置**：即使在 biography 合成路径，若 `characters.md` 字段（如 `公开身份` / `真实身份` / `角色关系图` / `称呼关系表`）或项目 state 的 `seedIdea` / `genre` / `tone` 明示含此类词（如题材 = 欢喜冤家、女主公开身份为"虐恋女主"），允许原样保留——以字段为权威，不触发禁用。SKILL.md `/导出` 人物小传合成步骤 4 规定"合成前先读字段再决定"。

**禁用词清单**（LLM 自由发挥合成时易触发的陈词）：

| 分类 | 命中词/写法 | 改法方向 |
|---|---|---|
| 相遇陈词 | 「命运般的相遇」「注定的相遇」「冥冥之中」「宿命的牵引」| 用具体场景与字段内容组合："在码头上第一次见，他伸手扶她下船" |
| 张力陈词 | 「暗流涌动的张力」「若即若离的默契」「欲说还休的情愫」| 用动作与对白："她转身就走，他没喊她"|
| 羁绊陈词 | 「千丝万缕的联系」「纠缠不清的过往」「扯不断的羁绊」| 用字段关系直陈："她是他的救命恩人"|
| 关系标签陈词 | 「欢喜冤家」「相爱相杀」「虐恋情深」| 见上方 Exception。若字段明示则允许原样，否则用"互相嫌弃" / "互相伤害" 等描述 |

**判定逻辑**：身份与关系段必须基于 `公开身份` / `真实身份` / `角色关系图` / `称呼关系表` / `核心动机` / `冲突点` 字段的客观陈述，不得添加字段未出现的情感修辞。题材术语（如题材 = 欢喜冤家）和角色标签字段可以原样出现，不视为违禁。

---

## 上下文连贯性

- 写第 N 集前，回顾前面已完成的集数内容
- 确保角色行为与 characters.md 一致
- 确保剧情推进与 episode-directory.md 一致
- 确保角色称呼与 characters.md 称呼关系表一致（尤其注意层级称呼）
- 确保台词风格与 characters.md 角色-语言风格映射表一致
- 如发现前后矛盾，主动提醒用户

---

## 已用台词库（跨集去重机制）

**文件**：`used-lines.md`（项目根目录，和 `episode-directory.md` 同级）。详细格式/加载/扫描/反模式规则见 `used-lines-protocol.md`（生成分集前/后必读）。

### 核心约束

- **写第 N 集前**：`/分集` 命令强制 Read `used-lines.md`（存在则读，不存在视为空）。加载策略按集数分档（N≤10 全量 / 10<N≤30 最近 10 集全量+更早仅标题及前 2 条 / N>30 更激进裁剪），详见 `used-lines-protocol.md#读取规则`
- **语用框架**：这不是"don't 式否定约束"，是**"已用清单，从候选集剔除"的正向召回**。生成情绪锚句/遗言/口头禅等类别时，优先选新表达而非复用已用清单中的
- **写完第 N 集**：`/分集` 命令强制追加 3-5 条本集"高复读风险台词"到 `used-lines.md`（Read-Modify-Write，不得 overwrite）。判定标准：情绪锚句 / 遗言告别 / 口头禅 / 3 字以上标志性形容词组合 / 特殊意象。不追加功能性台词（"走吧"、"知道了"）
- **/自检 第 5 维度**：连贯性扫描新增子项"跨集台词复用扫描"——本集关键台词 grep `used-lines.md`，命中 ≥1 次扣 2 分，≥3 次扣 5 分且该维度上限 3 分
- **允许的例外**：角色口头禅（≤3 集间隔）/ 世界观专业术语 / 明确设置的 callback 意图——由 `/自检` 在 findings 中标 `[意图复用]` 放行，但必须给具体理由

### 反模式禁区

- ❌ 凭记忆冒充扫描（必须真实 Read used-lines.md）
- ❌ 每集追加 <3 条敷衍
- ❌ 把全部对白写进 used-lines.md（稀释正向召回信号）
- ❌ overwrite 整个文件
- ❌ 用"don't 用 XX 台词"措辞

---

## 上下文窗口管理（50+ 集长剧）

写后续集数时，上下文不够装全部已完成集数。Phase 1A 起优先使用 `continuity-ledger.md` 作为剧情动态记忆，不再默认把全部历史正文塞进上下文。

| 内容 | 加载方式 | 始终加载 |
|------|---------|---------|
| creative-plan.md | 全文 | [完成] |
| characters.md（含语言风格表） | 全文 | [完成] |
| episode-directory.md | 全文 | [完成] |
| continuity-ledger.md | 全文 | [完成] |
| used-lines.md | 按 used-lines-protocol 分档 | [完成] |
| 最近 3 集完整剧本 | ep{N-3} ~ ep{N-1} 全文 | [完成] |
| ledger 指向的相关埋设集 | CONTINUITY 优先，必要时全文 | 按需 |
| 其余已完成集数 | 不默认读取正文 | 否 |

**执行规则**：
- 当 N ≤ 10：可加载全部已完成集数，但仍必须更新 `continuity-ledger.md`
- 当 N > 10：不得默认加载全部历史正文；先读 ledger，再按索引精准读取
- 批量写 `/分集 5-8` 时：每写完一集，先更新 `continuity-ledger.md`，再写下一集
- 发现需要回溯远期集数时，必须说明触发原因：主线伏笔取证、角色状态冲突、尾钩义务、导出梗概证据不足
- 老项目缺 `continuity-ledger.md` 且已有完成集时，先按 `continuity-protocol.md#老项目-bootstrap` 生成轻量台账，再继续 `/分集 next`

---

## 创作原则

1. **渐进式创作：** 每一步确认后才进入下一步，不一口气生成不可控的内容
2. **可随时调整：** 任何阶段可以回头修改，修改后自动更新下游内容
3. **上下文连贯：** 写后续集数时必须参考已有内容，避免前后矛盾
4. **质量可控：** 每集写完可跑自检，不满意就重写
5. **专业格式：** 输出的是文学剧本格式，导演拿到手能直接拍

### 男主不互骂硬规则
<!-- 来源：Gemini方法论融合 2026-04-23 -->

**核心：** 主角绝不与反派进行对等的言语互骂。这会把主角降格为同层次的人。

| 场景 | ❌ 错误写法 | ✅ 正确写法 |
|------|-----------|-----------|
| 反派辱骂主角 | 主角反骂回去，言辞对等 | 主角沉默/轻笑，一句话切断对话 |
| 反派挑衅 | 主角被激怒，口头对战 | 主角用行动碾压，不在言语上纠缠 |
| 反派发表长篇嘲讽 | 主角逐条反驳 | 主角一个动作让反派哑口（铁证/身份揭露）|

**设计逻辑：** 言语对战 = 同一层级。主角的降维打击不在嘴上，在认知差距上。观众爽点是看主角碾压，不是看主角"吵赢了"。

---

## 考据可追溯性自检流程（厚型/中型题材）

`/自检` 第 8 维度的核查流程：

1. 检查 `setting-bible.md` 是否存在
   - 不存在 + 题材为厚型 → 第 8 维度 0 分，提示「请先 /考据 auto」
   - 不存在 + 题材为中型 → 第 8 维度 0 分，但允许用户跳过（标记 `bibleScope: ["user-declined"]`）
   - 不存在 + 题材为轻型 → N/A 不计入

2. 提取剧本中所有"具体专业事实"，逐条对照 bible：
   - 专业术语 / 官名 / 制度名 / 药物名 / 法条编号 / 监管机构 / 数字精度（年份/剂量/能量参数）
   - 形式化检索：见 `quality-rubric.md#编造检测启发式` 的正则模式
3. 对每条事实判定：
   - 在 bible 且 verified → ✅ 通过
   - 在 bible 但 [待人工核源] → ⚠️ 需在剧本中加 `[考据待定]` 标注
   - 不在 bible 但应在 → ❌ 编造，扣分
   - 不在 bible 但属于「虚构白名单」→ ✅ 通过

4. 时代/领域常识扫描（按 `setting-bible.md` 时代/场景常识锚点字段）：
   - 称谓 / 饮食 / 服饰 / 礼仪 / 工具
   - 命中"已知雷区"字段 → 上限 6 分；命中 ≥2 → 上限 3 分

5. 制度规则一致性扫描：前后集出现相同制度引用时，必须遵守相同规则

详细分数锚点见 `quality-rubric.md#八-考据可追溯性`。

---

## 自检维度细则

8 维度检查（轻型题材跳过第 8 维度），每项 1-10 分（爽点 1-15，格式 1-5）：

| 维度 | 检查内容 |
|------|---------|
| 节奏 | 开场是否够快、有无拖沓段落、紧张-舒缓交替是否合理、**[锚点] 三锚点检查**：0-3s 冲突点是否在前 3 段内、30s 情绪爆破是否存在、结尾钩子是否标记 [锚点]——缺少任意一个锚点该维度不超过 5 分 |
| 爽点 | 数量是否足够（默认目标 ≥3 个；不足标留存风险，传统爽文/IAA 路径可限制分数上限）、强度是否达标、类型是否多样、是否对照 `commercial-ledger-cn.md` 写清蓄力 / 兑现 / 余震，避免只标爽点名不兑现 |
| 台词 | 有无废话、角色区分度、是否口语化自然、**称呼是否匹配层级关系、场景台词是否符合场景逻辑**（参考 realism-checklist.md）、**每句台词是否符合角色-语言风格映射表和声音指纹**、**潜台词检查：是否把情绪/动机说太满**、**动作替代检查：能用动作/道具/沉默替代的对白是否仍在解释**、**台词节奏检查：逐条扫描，标出连续 3 句以上或单行约 90 字以上的台词并判断是否需要拆分** |
| 格式 | 场景头完整性、景别标注、音乐提示、特殊标记、**输出语言一致性**（全文与 `.drama-state.json` 的 `language` 一致；出海中文呈现允许英文角色名/专有名词）、**对白段落边界**（剧本正文内每个 `**角色名**` cue 独立成源 Markdown 段落；同段第二个 cue 判格式错误）、**模板合规**（不含模板外区块如导演手记/创作心得）；**新格式建议扫描**（见下方「新格式建议扫描」说明，0 扣分仅作【建议】标签 findings）|
| 连贯性 | 与前后集是否矛盾、角色行为是否一致、伏笔是否延续、**认知转变是否有具体触发物、情绪转折是否有过渡状态、关系呈现是否生活化**（参考 realism-checklist.md）、国内模式下本集买单理由是否承接 `creative-plan.md#商业账本` 与目录版商业职责，反派压力是否持续升级 |
| 反抽象 | **△ 段落是否有纯情绪词未转为物理动作**（扫描全文，逐个标出「她很伤心」「气氛凝重」类描述并给出物理化替换建议） |
| AI Slop | **台词书面化**：扫描全部台词，标出「我认为我们应该」「在某种程度上」「不可否认」等书面表达，给出口语化替换；**情绪转变过平滑**：检查角色情绪/立场转变是否缺少抗拒、犹豫、反复等中间状态（「她听完立刻原谅了他」= AI Slop）；**巧合堆砌**：统计「正好」「恰好」「刚好」「碰巧」「没想到」出现次数，全集 ≥3 次判定为巧合过多，每处标出并建议用因果逻辑替代。**【硬约束 v1.18.2】**：≥3 次 → AI Slop 维度 ≤6 分；≥5 次 → ≤4 分（封顶上限，与分数锚点取较低分） |
| 考据可追溯性 | **题材分化**：厚型/中型计入总分（满分 80），轻型 N/A（满分 70）。**核查流程**：见上方「考据可追溯性自检流程」。**雷区否决**：命中 1 条雷区上限 6 分，≥2 条上限 3 分。详见 `quality-rubric.md#八-考据可追溯性` |

---

## --fix 模式流程

1. 先由独立质检 agent 完成完整自检，生成评分报告和问题清单（与普通模式相同；调度端不得自评）
2. 若涉及台词维度，先执行 `dialogue-craft-cn.md#自检---fix删改顺序`：30% 删除 → 大声读 → 节奏扫描 → 潜台词补回。优先删或动作化，禁止只做同义润色
3. 逐项展示每个【严重】问题的定位和修复选项（A/B）
4. 用户对每项选择：A（应用方案A）/ B（应用方案B）/ 跳过
5. 直接修改 `episodes/ep{NNN}.md` 源文件（非副本）
6. 全部修复完成后，重新启动独立质检 agent 复审并更新分数；调度端不得自行宣布通过

### 考据缺失补丁（--fix 子流程）

第 8 维度扣分项的 --fix 处理：

- **专业事实未映射 bible** → 选项 A：调用 /考据 auto 增量检索补 bible 字段；选项 B：改为模糊表述；选项 C：标 `[虚构]` 加入白名单
- **命中已知雷区** → 选项 A：按 bible「已知雷区」字段重写该段；选项 B：删除该段
- **bible [待人工核源] 字段被引用** → 选项 A：加 `[考据待定]` 标注（不扣分但提示用户）；选项 B：改为已 verified 字段

---

## 分数持久化

自检完成后，将每集总分写入 `.drama-state.json` 的 `qualityScores` 字段（格式：`{"1": 52, "2": 61, ...}`）。`/导出` 依赖此数据做质量门控。

---

## 新格式建议扫描（v1.15.8，0 扣分软约束）

自检时扫描以下 3 类"老格式"模式。命中 → 以【建议】标签追加到 `## 问题清单` section（与【严重】条目同位列），**不进入扣分计算**、不影响总分、不阻断 /导出。

| 模式（命中即提醒）| findings 条目文案模板 |
|---|---|
| 场景头非 `内/外 · 地点 · 日/夜` 顺序（含老格式 `日/夜 内/外 {地点}`）| 【建议】场次 X 场景头建议改为「场号 · 内/外 · 地点 · 日/夜」顺序（行业通用写法）|
| `**角色名（OS）**：` / `**角色名（VO）**：`（OS/VO 技术标记被一起加粗）| 【建议】场次 X OS/VO 粗体建议只包裹角色名：`**角色名**（OS）：`（OS/VO 不加粗便于扫读） |
| `[音乐] ` 前缀（老格式音乐标注）| 【建议】场次 X 音乐标注建议用 `（BGM：氛围描述）` 或 `（音效：具体音效）` |

### 对白段落边界扫描（剧本正文强制）

自检时只扫描剧本正文：从每集第一个场景标题开始，到 `CONTINUITY` / `<!-- 剧本正文到此结束 -->` / 考据附录前结束。按空行切分源 Markdown 段落；单换行不算新段落。若任一段落出现 2 个及以上 `**角色名**：` / `**角色名**（...）：` / `**角色名**（OS）：` / `**角色名**（VO）：` cue，判为【严重】格式错误。修复方式：若多句属于同一角色的连续发言，合并为单条台词 cue，不重复 speaker cue，并删除机械重复的括号动作；需要节拍停顿时插入 `△` 动作行再另起 cue；只有确属不同角色或 cue 形态切换（台词/OS/VO）的，才拆成独立对白段。同形态同名 cue 直接相邻（逐句重复角色名）同样判【严重】。同声、重叠、群众反应用 `△` 动作段说明，或使用 `**众人**：` 聚合 cue。引用块、表格、模板示例、前情提要、自检报告、问题样例、考据附录和其他非剧本正文内容不触发。

### 实现要点

- **prompt-side 扫描**：LLM 读全集原文，按模式识别（跨模型兼容，不依赖 grep/脚本）
- **0 扣分**：「格式与可拍性」维度总分不受新 3 规则影响（保留原有破折号禁用 / AI 误用等扣分）
- **向后兼容**：老集数（v1.15.7 及之前生成）命中老格式会出现【建议】条目但不扣分，作者可选择忽略或重写
- **设计哲学**：生成层 prompt 硬约束新格式（SKILL.md「新格式规范」段），审查层软约束（本段），让历史剧本过渡期友好

### 不触发场景

- Hollywood 英文交付格式的 `[音乐] Music cue:` 保留
- Hollywood 英文交付格式场景头 `INT./EXT. LOCATION - DAY/NIGHT` 不触发（英文好莱坞标准另行处理）
