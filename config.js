const $=s=>document.querySelector(s);function e(v=''){return String(v).replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));}function pill(v){return `<span class="state-pill ${v?'ok':'bad'}">${v?'已配置':'未配置'}</span>`;}function card(name,obj){return `<article class="monitor-card config-card"><div><strong>${e(name)}</strong></div>${Object.entries(obj||{}).map(([k,v])=>`<p><b>${e(k)}</b> ${typeof v==='boolean'?pill(v):e(v??'')}</p>`).join('')}</article>`;}async function load(){const d=await fetch('/api/config/status').then(r=>r.json());$('#configUpdated').textContent=new Date().toLocaleString('zh-CN',{hour12:false,timeZone:'Asia/Shanghai'});$('#configGrid').innerHTML=[card('App',d.app),card('企业微信',d.wechat),card('OCR',d.ocr),card('模型网关',d.gateway),card('助手',d.assistant),card('自动备份',d.backup),card('通知订阅',d.notifications),card('知识库',d.knowledge)].join('');}$('#refreshBtn').onclick=load;load();
